/** @type {import('next').NextConfig} */
const nextConfig = {
    // If you plan on hosting the frontend and backend on different domains,
    // you might need to configure rewrites here to avoid CORS issues in production.
    // For Discord's URL Mappings, this might not be necessary.
};

export default nextConfig;
